/*package student_sort;

import University.Student;

import java.util.ArrayList;
import java.util.Comparator;

/**
 * Created by Алан on 27.12.14.
 */
/*public class SortStudentAverageMarks implements Comparator<Student> {
    @Override
    public int compare(Student o1, Student o2) {
        if (getAverage(o1.getMarks()) < getAverage(o2.getMarks())) {
            return 1;
        }
        if (getAverage(o1.getMarks()) > getAverage(o2.getMarks())){
            return -1;
        }
        return 0;
    }

    public Integer getAverage(int[] marks) {
        int result = 0;
        for (int i = 0; i < marks.length; i++) {
            result += marks[i];
        }
        return result / marks.length;

    }
}
*/